// Palette TRAVEX GLOBAL — basée sur le modèle Treliko
export const colors = {
  primary: '#1D5A9E',      // bleu principal (boutons, titres)
  primaryDark: '#16497F',  // bleu foncé (en-têtes)
  primaryLight: '#E8F0FA', // bleu très clair (fonds, hover)
  orange: '#F0A020',       // accent orange (badge NOUVEAU)
  green: '#27A463',        // vert (Voyageur, Contacter)
  red: '#D9534F',          // rouge (interdits, déconnexion)
  bg: '#F1F3F6',           // fond de l'app
  card: '#FFFFFF',         // fond des cartes
  text: '#1F2937',         // texte principal
  muted: '#6B7280',        // texte secondaire
  border: '#E7EAEE',       // bordures
  inputBg: '#F3F5F7',      // fond des champs
  star: '#F5A623',
  white: '#FFFFFF',
};

export const spacing = {
  xs: 4, sm: 8, md: 12, lg: 16, xl: 20, xxl: 28,
};

export const radius = { sm: 10, md: 14, lg: 18, xl: 24 };

export const shadow = {
  card: {
    shadowColor: '#0B2545',
    shadowOffset: { width: 0, height: 3 },
    shadowOpacity: 0.06,
    shadowRadius: 10,
    elevation: 2,
  },
};
